# Agent Project Notes

The Study Research Agent reads local files and converts them into document chunks.

The search tool ranks evidence by comparing important words from the question with words in each chunk.

The calculator tool is used when the user includes an arithmetic expression such as a budget total or score average.

Deployment is prepared as a local command-line application with package metadata and test instructions.
