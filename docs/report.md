# Study Research Agent Report

## Final System Description and Goal

Study Research Agent is a Python AI-assisted command-line system. Its goal is to help a user analyze study notes, project files, CSV records, or JSON data by using tools during execution. The user gives a question, a file path, and optionally a calculation. The agent reads and converts the file, searches for relevant evidence, calculates when requested, and returns a meaningful answer. When `OPENAI_API_KEY` is configured, the agent also uses the OpenAI API to synthesize the final answer from the tool results.

## AI or Agent-Based Approach

The system uses a single-agent workflow. The `StudyResearchAgent` decides which tool calls are needed based on the user input:

- If a file is provided, it calls the file reader tool.
- If a question and readable file chunks are available, it calls the text search tool.
- If an arithmetic expression is provided, it calls the calculator tool.
- If an API key is configured and `--offline` is not used, it calls the OpenAI synthesizer tool.
- It synthesizes the final response from the tool outputs, either locally or with the API.

This is an agent-based design because the central component coordinates multiple tools to solve a user request instead of using one fixed function. It is also AI-based when OpenAI mode is enabled because the final answer is generated by a language model.

## Programming Concepts Used

- Modular project structure: source code is separated into agent, CLI, models, and tools.
- Classes and dependency injection: each tool is a class, and the agent can receive custom tool instances for testing.
- Dataclasses: `ToolResult`, `DocumentChunk`, and `AgentResponse` keep data consistent between components.
- Error handling: unsupported files, missing files, invalid calculations, missing API keys, and empty input are handled explicitly.
- Safe parsing: the calculator uses Python AST nodes instead of `eval`.
- API integration: the optional OpenAI synthesizer reads configuration from environment variables and fails safely when unavailable.
- File I/O and data conversion: text, Markdown, CSV, and JSON are normalized into a shared chunk format.
- Automated testing: `unittest` verifies tools, workflow, validation, API fallback, and CLI output.

## Tools and Their Roles

`FileReaderTool` reads external local files. It supports `.txt`, `.md`, `.csv`, and `.json`, then converts them into `DocumentChunk` objects.

`TextSearchTool` acts as a retrieval tool. It tokenizes the user question and document chunks, ranks chunks by term overlap, and returns the most relevant evidence.

`CalculatorTool` evaluates arithmetic expressions safely. It supports common arithmetic operators and rejects unsafe Python expressions.

`OpenAISynthesizerTool` optionally calls the OpenAI Responses API. It receives the user question, local draft answer, evidence, and warnings, then asks the model to produce a concise final answer without inventing facts.

## Input, Output, and Data Conversion

Input can be:

- A natural-language question.
- A local file path.
- An arithmetic expression.
- A request for text or JSON output.
- Optional local-only mode through the `--offline` flag.

The data flow is:

1. CLI parses the input.
2. The agent chooses required tools.
3. The file reader converts external file formats into `DocumentChunk` records.
4. The search tool ranks chunks and returns evidence.
5. The calculator returns a numeric result when requested.
6. The OpenAI synthesizer rewrites the final answer if `OPENAI_API_KEY` is configured.
7. The agent produces an `AgentResponse`.
8. The CLI prints text or JSON.

Correctness is preserved by keeping source path and chunk number inside every evidence item. The OpenAI prompt receives only the local draft answer, evidence, and warnings, which reduces the risk of unsupported claims.

## Testing Results and Conclusions

Testing was done with the standard-library `unittest` module. The test suite includes scenarios for:

- Valid calculation returning the correct number.
- Unsafe calculation rejection.
- Division-by-zero error handling.
- Markdown, JSON, and CSV file conversion.
- Missing file handling.
- Relevant evidence ranking.
- Queries without useful search terms.
- Agent answers from file evidence.
- Agent combines file evidence and calculation.
- Empty input validation.
- CLI JSON output.
- AI synthesis after local tools.
- Safe fallback when AI synthesis fails.
- Missing OpenAI API key reporting.

Final local result: all tests passed.

## Deployment Preparation

The project includes:

- `README.md` with installation, usage, testing, and deployment instructions.
- `requirements.txt` showing that offline mode has no runtime dependencies and AI mode has an optional OpenAI dependency.
- `.env.example` showing the API configuration format without exposing secrets.
- `pyproject.toml` for editable installation, optional dependencies, and a `study-agent` console command.
- `docs/deployment.md` with setup, launch, configuration, verification, and release notes.

## Proposed Deployment Strategy

The best initial deployment is a local command-line application. This is suitable because the system reads local files and should protect user data. A staged release is recommended: test locally, push to GitHub, tag a version, share with a small user group, and only later consider API or web deployment.

AI mode should be enabled through explicit configuration. Users set `OPENAI_API_KEY`, and the project continues to support offline fallback for testing and demonstrations through `--offline`.

## Journal

### Step 1 - 24.04

Planned system: an AI-assisted study and project research agent that answers questions from local files and performs calculations when needed.

Planned AI approach: one coordinating agent that chooses tools based on the user request, with optional LLM synthesis as a possible extension.

Planned tools:

- File reader for local notes and structured files.
- Text search/retrieval for evidence.
- Calculator for numeric questions.
- Optional OpenAI API synthesizer for final natural-language answers.

Preliminary programming concepts:

- Python modules and packages.
- Classes for agent and tools.
- File input/output.
- Data structures for tool results.
- Error handling.
- Basic tests.
- API configuration through environment variables.

### Step 2 - 08.05

Updated implementation progress: the project now has a package structure with agent, tools, CLI, and shared models. The implementation supports local file reading, evidence search, safe calculation, and optional OpenAI API synthesis.

Refined programming concepts actually used:

- Dataclasses for structured data exchange.
- Dependency injection for testable agent tools.
- AST parsing for safe calculator behavior.
- CSV and JSON conversion into normalized text.
- CLI argument parsing with `argparse`.
- Environment variables for optional API configuration.

How tools are integrated: the agent receives the user request, calls `FileReaderTool` if a file is present, passes the resulting chunks to `TextSearchTool`, calls `CalculatorTool` if a calculation is present, and joins the outputs into one response. If `OPENAI_API_KEY` is configured, `OpenAISynthesizerTool` creates the final API-backed answer from the same evidence.

### Step 3 - 15.05

Testing process: tests were added during implementation instead of only at the end. Tool tests verify each tool separately, while workflow tests verify the full agent and CLI.

Test scenarios:

- Valid calculation returns the correct number.
- Unsafe calculation is rejected.
- Missing file returns a readable error.
- Markdown, CSV, and JSON inputs are converted correctly.
- Search returns the most relevant chunk first.
- Empty user input is rejected.
- CLI JSON output can be parsed.
- Agent combines evidence and calculation in one answer.
- Optional AI synthesis works after local tools.
- Missing API configuration is handled as a warning.

Deployment preparation: the system can be run with `python main.py` or installed as `study-agent` through `pyproject.toml`. AI mode additionally requires the optional `openai` package and `OPENAI_API_KEY`, which can be stored in a local `.env` file.

Data conversion: file inputs are normalized into `DocumentChunk` records. JSON is parsed and formatted consistently; CSV rows become key-value text; plain text and Markdown are cleaned and chunked.

### Final Submission - 22.05

Final result: the system is a functional AI-assisted agent that uses external tools during execution. It receives user input, calls tools, handles errors, and returns a useful response with evidence. It can run offline for deterministic testing or use a real OpenAI model when configured.

Final programming concepts: modular architecture, classes, dataclasses, file processing, structured error handling, safe AST parsing, API integration, CLI design, and automated testing.

Final tools: file reader, text search, calculator, and optional OpenAI synthesizer.

Final testing conclusion: all tests pass in the local test environment.

Final deployment preparation: README, dependency file, package metadata, console command, `.env.example`, deployment notes, optional AI configuration, and test instructions are included.

Final deployment strategy: local CLI first, optional API mode for users with an OpenAI key, then staged GitHub releases after test verification.
